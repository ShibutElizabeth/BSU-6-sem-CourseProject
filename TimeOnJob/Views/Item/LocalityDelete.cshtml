@model Alia.Models.Locality
@{
    ViewBag.Title = "Removal locality";
}

<div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
        <div class="modal-body">
            <table>
                <thead>
                    <tr><th>id</th><th>name</th><th>product type id</th></tr>
                </thead>
                <tbody>
                    <tr>
                        <td>@Html.DisplayFor(model => model.LocalityId)</td>
                        <td>@Html.DisplayFor(model => model.LocalityName)</td>
                        <td>@Html.DisplayFor(model => model.RegionId)</td>
                    </tr>
                </tbody>
            </table>
            @*<dl class="dl-horizontal">
                <dt>id</dt>
                <dd>
                    @Html.DisplayFor(model => model.LocalityId)
                </dd>

                <dt>name</dt>
                <dd>
                    @Html.DisplayFor(model => model.LocalityName)
                </dd>

                <dt>product type id</dt>
                <dd>
                    @Html.DisplayFor(model => model.RegionId)
                </dd>
            </dl>*@

            <div>
                <form asp-controller="Item" asp-action="LocalityDelete" method="post">
                    <div class="form-group">
                        <button type="submit" class="btn btn-default">Delete locality</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>